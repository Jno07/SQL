<?php	
	//Code d'ouverture de la base selon l'environnement de travail (local ou distant)
	if($_SERVER['HTTP_HOST'] == 'localhost'){
		try{
			$db = new PDO('mysql:host=localhost;dbname=DATABASE', 'root', '');
		}catch (Exception $e){
			echo 'Impossible de se connecter à la base de donnée. Veuillez <a href="contact.php">contacter l\'administrateur</a>';
			die();
		}
	}else{
		try{
			$db = new PDO('mysql:host=HOST;dbname=DATABASE', 'USER', 'PASSWORD');
		}catch (Exception $e){
			echo 'Impossible de se connecter à la base de donnée. Veuillez <a href="contact.php">contacter l\'administrateur</a>';
			die();
		}
	}
$db->exec("SET CHARACTER SET utf8");
$db->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_WARNING);
?>

Remarque : Penser à fermer chaque requête après utilisation avec la fonction closeCursor()
